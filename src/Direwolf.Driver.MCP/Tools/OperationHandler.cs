using System.Text.Json;
using Direwolf.Definitions;
using Direwolf.Definitions.Enums;
using Direwolf.Definitions.LLM;

namespace Direwolf.Driver.MCP.Tools;

//TODO: Create handlers
public static class OperationHandler
{
    // public static async Task<object> HandleCreateWolfpack(object args)
    // {
    //     var json = JsonSerializer.Serialize(args);
    //     var data = JsonSerializer.Deserialize<JsonElement>(json);
    //     
    //     var wolfpack = Wolfpack.Create(
    //         RequestType.Get,
    //         data.GetProperty("name").GetString(),
    //         data.TryGetProperty("properties")
    // }
}